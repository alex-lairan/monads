# Examples

Worked examples showing how the monads fit together in real code.

- [Error Handling with Either](/examples/error-handling) — model failable
  operations and recover at the end of a chain.
- [Avoiding nil with Maybe](/examples/avoiding-nil) — replace `nil` checks with a
  safe, composable pipeline.

For a method-by-method tour of the shared interface, see
[Chaining Monads](/guide/chaining).
